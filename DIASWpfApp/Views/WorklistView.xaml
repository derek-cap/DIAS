<UserControl x:Class="DIASWpfApp.Views.WorklistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DIASWpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Button Command="{Binding SearchCommand}" IsEnabled="{Binding IsSearchEnabled}" Width="96" Height="32" Margin="4,4,16,4" HorizontalAlignment="Right">Search</Button>
            <ListBox ItemsSource="{Binding Patients}" MinHeight="400"></ListBox>
        </StackPanel>
        
    </Grid>
</UserControl>
